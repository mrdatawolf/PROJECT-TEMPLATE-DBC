[Slide 25 — scales down too]

And it's not just for projects with eleven contracts and a governance
panel. DM_Helper is a hobbyist app, day one of adoption, same discipline,
much lower stakes — and it already caught something worth catching. A
task that read as a plain "consolidate the auth checks" refactor request
turned out to be hiding a genuine product decision about DM/admin
equivalence. The task file says it outright: don't resolve it by guessing
which behavior "seems right" — escalate if it isn't already unambiguous.
Caught before it silently became a permissions redesign, on a hobby
project, on day one. The process doesn't need scale to pay for itself.