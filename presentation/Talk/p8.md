**[Slide 8 — Parallel pipelines]**
Before we scale out, notice where the grid itself came from — a human and
an AI, paired, writing the contracts that populate it. That's not new
information, it's the same pair from the last panel, just finally drawn in
as the source instead of implied. Now replicate that single-item pipeline
across many work items at once. Parallelism is what actually clears a
backlog — this is the scale-out step, governance hasn't entered the
picture yet.