## Close

[Slide 27 — start here]

If you want to try this: read `CLAUDE.md` for the foundational rules,
read `AGENTS.md` for the operating instructions agents actually follow,
read `docs/AI_DEVELOPMENT_SYSTEM.md` for the philosophy in full, then ask
your AI to draft a task for something you actually want to build and see
what comes back. It's a template repository — clone it, gut the parts
that don't fit your team, keep the gates.

And if you remember one thing on the way out: the discipline was never
optional, we just used to be able to get away with skipping it because
rebuilding was slow enough to force a second look eventually. That safety
net is gone. Write the decision down once, precisely, as a contract — and
then let execution be as cheap and disposable as it's ever going to get.