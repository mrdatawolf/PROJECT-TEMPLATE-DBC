Read the deck. Slide 5 (index 5, "Specialize the steps, not just the person") uses panel-3-ai-factory.png, a flat raster illustration — station 2 ("Coding / Codex") is exactly the slot you're describing.

On animating it: doable, but since it's a flat PNG there's no "animate the image" shortcut — you'd overlay an HTML/CSS element positioned on top of just that one icon box, and crossfade through Codex → ChatGPT → a man → a woman icons with a CSS keyframe loop (percentage-positioned so it scales with the image). Main risk is getting that overlay pixel-aligned with the illustration so it reads as native to the image instead of a sticker slapped on top, especially at the phone breakpoint. The more robust alternative is redrawing that whole panel natively in HTML/SVG instead of a raster export — more work, but no alignment fights and it'd match the crisp terminal aesthetic the rest of the deck uses after slide 9 (worth noting: panels 1–6 are in a different, illustrated-cartoon style from everything after — that seam already exists). I'd lean overlay-on-existing-PNG first since it's the cheaper bet, and only rebuild natively if alignment proves annoying.

On whether you're focusing on the wrong part first: I don't think you're wrong. The sharpest, least-obvious claim this deck could make is exactly what you said — think hard once (the contract), then execution is fast and disposable, and because the contract outlives the implementer, you can swap in a different implementer (AI or human) and iterate by writing a new contract rather than re-litigating the old one. That idea barely exists in the current deck; it's implied by the "AI's edge is speed" aside on slide 5 and the governance panel on slide 8, but it's never stated as the payoff. Slide 22 ("what this actually buys you") lists bounded scope, reviewable diffs, durable knowledge — but not "cheap iteration via fresh contracts," which sounds like your actual thesis.

I wouldn't cut the factory panels though — they're good scaffolding for making "any station is human-or-AI-interchangeable" concrete before you ever say the word "contract," and the animation you're describing would make that point even harder to miss. I'd keep them, but state the front-load-then-iterate idea explicitly and earlier — either in the title subtitle or as a short beat right after slide 8, before the deck pivots into DbC vocabulary — instead of leaving it implied.

Want me to draft that explicit beat (as a slide or a subtitle rewrite) and prototype the slide-5 animation, or do you want to react to the framing first?

