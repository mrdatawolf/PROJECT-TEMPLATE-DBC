# Architecture

Document the current system architecture here after it has been approved.

Include system boundaries, major components, dependencies, data flows, trust
boundaries, and links to relevant ADRs. Describe the current architecture rather
than proposals; proposed changes belong in tasks and contracts until accepted.
